# コスめぐり 設計書

レイヤーの一日エージェント（CANDIDATE 13）

## 1. 概要

コスプレイヤーの遠征を丸一日支える統合エージェント。ウィッグ・衣装のバーチャル試着、キャラ再現メイクの手順化、大荷物制約の移動動線、更衣室の混雑予測、合わせ（グループ）のオーケストレーションを一つに束ねる。

- **一言価値**: XのDMとスプレッドシートで回っている「合わせの幹事業務」と、紙と行列のままの「更衣室運用」をエージェント化する
- **テーマ適合**: 「素顔とコス名を紐づけない」というコミュニティ固有のプライバシー要求を、設計の中心に置く
- **市場性**: 世界コスプレサミット（名古屋、40ヶ国以上参加）・acosta!・コミケなど恒常的な大型イベント群に加え、コスプレ目的の訪日（コスプレインバウンド）が拡大中。**多言語・多人種対応**（肌色・骨格の違いに合わせたメイク工程生成）を最初から設計に組み込み、海外レイヤーを一次ターゲットに含める

## 2. 課題

| 課題 | 現状 |
|---|---|
| 更衣室が紙と行列 | 大型イベントは当日券＋長蛇の列。枠予約も混雑予測もない（コミケ・acosta等で確認） |
| メイクが参入障壁 | AIコスプレ「画像生成」は多数あるが、自分の顔に施す**手順**を教えるものは皆無 |
| メイク情報が日本人前提 | 既存のコスメイク情報は日本人の肌色・骨格を暗黙の前提にしており、肌色が濃い・彫りが深いレイヤーはそのまま真似できない。海外レイヤーの実課題 |
| 大荷物移動が過酷 | キャリー＋ウィッグ＋小道具。一般の経路案内は身軽な人前提 |
| 合わせ幹事が手作業 | 進捗・集合・撮影枠・カメラマン調整がDMとスプレッドシート |
| 訪日レイヤーの情報障壁 | WCS等で来日する海外レイヤーは、更衣室ルール・移動・イベント慣習を母語で得る手段がない |

## 3. ユースケース

1. **ソロ遠征**: 試着→メイク手順ナビ→大荷物動線→更衣室混雑を見た行動計画
2. **合わせ**: メンバー招集→全員の進捗管理→当日の到着監視→撮影枠リスケ
3. **訪日レイヤー（インバウンド）**: 世界コスプレサミット（名古屋）等への海外からの参加者を母語で伴走。空港→ホテル→会場の大荷物動線、更衣室ルール・イベント慣習の案内、肌色・骨格に合わせたメイク工程
4. **イベント主催者**（拡張）: 更衣室の枠管理・混雑ダッシュボード、多言語案内の自動生成

### 対応イベント（初期収載）

| イベント | 場所 | 特性 |
|---|---|---|
| 世界コスプレサミット（WCS） | 名古屋・大須 | 40ヶ国以上、インバウンドの本丸。市街地回遊型で移動動線が複雑 |
| acosta! | 池袋・ハレザ等 | 高頻度開催。更衣室枠・街歩き動線 |
| コミックマーケット | 東京ビッグサイト | 最大規模。更衣室行列・大荷物・帰路混雑 |
| ホココス | 名古屋・栄 | 道路開放型。地元イベントとして土地勘あり |

## 4. システムアーキテクチャ

```
[PWA (コス名アカウント)]
  ├── ログイン画面（自作UI）──── Firebase Authentication（IDトークン取得）
  ├── チャット欄（自作UI）
  └── お知らせ欄（自作UI）        # 外部メッセージングは使わない
      │  REST + IDトークン
      ▼
[API Gateway (Cloud Run: api)]   # トークン検証 → コス名アカウントを解決
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── チャットエージェント ────── 自由文→条件抽出→不足分の聞き返し→プラン起動
  ├── 試着エージェント ────────── YouCam API (Hair Style/Color, Clothes Try-On)
  ├── メイクナビエージェント ──── YouCam API (AI Makeup / Look Try-On) + Gemini（工程分解）
  ├── 動線エージェント ────────── 駅すぱあと API MCPサーバー（大荷物制約）
  ├── 更衣室エージェント ──────── 混雑予測モデル（枠データ×イベント規模）
  ├── 合わせエージェント ──────── メンバー進捗・撮影枠のオーケストレーション
  └── 多言語エージェント ──────── Gemini（母語出力・文化的補足）
      │
      ▼
[Firestore]（イベント・合わせ・進捗・お知らせ・チャット / 終了後TTL削除）
[Cloud Storage]（一時画像。処理後に破棄）
[Cloud Logging]（監査ログ：画像破棄証跡・位置共有の有効期間・アカウント紐付け）
      ▲
[Cloud Scheduler] ── 当日モードの自律進行（その日の遠征をまとめて進める）
                  └─ TTL削除（位置・進捗）
                     ※ どちらも共有シークレット（X-Tasks-Token）で保護
```

**外部メッセージングを使わない理由**: LINE・X 連携は身バレの経路を増やす。当日の自律通知
（遅延の再計算・撤収アラート・リスケの承認依頼）はアプリ内のお知らせ欄で完結させ、
宛先はコス名アカウントだけに保つ。電話番号・端末トークン・SNSアカウントを一切持たない。

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | 遠征計画の受付、当日モードの進行管理 | 対話＋当日は自律進行 |
| チャット | 自由文から条件（イベント・日付・作品/キャラ・出発駅・荷物）を抽出し、足りない項目だけを聞き返す。揃った時点で Orchestrator を起動 | 対話。**「次に何を聞くか」はLLMに委ねない**（聞き漏らしと堂々巡りの回避） |
| 試着 | キャラ→ウィッグ/衣装候補→試着画像 | 提案まで |
| メイクナビ | 目標キャラ×本人の顔立ち→工程分解→当日ステップ表示。**Fitzpatrick肌タイプ判定と顔属性・比率解析で、肌色（下地・カラコン発色・ハイライト位置）と骨格（彫りの深さ・鼻筋・目の間隔）に応じて工程を個別化** | 手順生成は自律 |
| 多言語 | 全UI・案内・メイク工程を利用者の母語で提供（日英中韓＋Geminiで随時拡張）。イベント慣習・更衣室ルールの文化的な補足説明も生成 | 自律 |
| 動線 | 大荷物制約の経路（EV優先・ロッカー・混雑回避）、遅延再計算 | 再計算・通知は自律 |
| 更衣室 | 混雑予測、入退場タイミング推奨、撤収アラート | 推奨まで |
| 合わせ | 進捗収集、到着監視、撮影枠リスケ起案 | **リスケ確定は主催者承認** |

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run、Cloud Scheduler（当日モードのバッチ・TTL削除） |
| AI（必須要件） | Gemini API（`gemini-3.7-flash`。メイク工程の言い換え・翻訳・チャットの条件抽出）、Agent Development Kit |
| スポンサー | YouCam API（Hair Style/Color VTO・Clothes VTO・AI Makeup/Look VTO・**AI Fitzpatrick Skin Type Analysis**・**AI Face Attributes & Ratio Analyzer**）、駅すぱあと API MCPサーバー |
| 多言語 | Gemini API（翻訳＋文化的補足の生成。UIはi18n、メイク工程は生成時に母語で出力） |
| 認証 | **Firebase Authentication**。ログイン画面は自作し、フロントは Identity Toolkit の REST でIDトークンを取得（JS SDK・CDN非依存）。バックエンドは firebase-admin で検証し、受け取るのは uid のみ |
| データ | Firestore（お知らせ・チャットも同居）、Cloud Storage（一時画像） |
| UI | PWA（HTML/CSS/JS、フレームワーク非依存）。ログイン・チャット欄・お知らせ欄はすべて自作 |

**外部メッセージング（LINE / X）は使わない。** 通知はアプリ内で完結させる（§7-6）。

## 6. データモデル（Firestore）

```
layers/{layerId}          # コス名のみ。本名・素顔と紐づけない
  handle, lang, prefs: { 移動制約, ホームイベント }
  face_profile: { fitzpatrick_type, 顔属性スコア }   # 数値のみ保持。画像は破棄
  auth_uid                # 認証基盤の不透明ID。**メールアドレスは保存しない**
                          # 未設定 = 合わせに招待されただけで本人未ログイン（pending）
expeditions/{expId}
  event: { 名称, 会場, 日時 }        # WCS/acosta/コミケ/ホココス等のマスタ参照
  event_date                        # 開催日（JST）。当日バッチが日付1本で引くための冗長列
  character: { 作品名, キャラ名 }   # メイク工程生成にのみ使用
  makeup_steps: [ {order, area, instruction, lang} ]   # 肌タイプ・顔属性で個別化
  route: { 行き, 帰り, 荷物モード }
awase/{awaseId}
  members: [ {layerId, 進捗, 到着見込み, 位置共有: 当日限定フラグ} ]
  shoots: [ {枠, 場所, カメラマン} ]
  ttl: イベント終了 + 24h で位置・進捗を自動削除
notifications/{notificationId}   # アプリ内お知らせ（LINEの置き換え）
  layerId, kind, message, read   # 宛先はコス名アカウントのみ。連絡先は持たない
chats/{layerId}                  # 1レイヤーにつき1本の対話
  messages: [ {role, text} ], slots: { 抽出済みの条件 }, exp_id
audit/{logId}  # 画像破棄・共有期間・リスケ承認・アカウント紐付けの証跡
```

イベントの時刻はすべて **JST 基準**で組み立てる。収載イベントは日本開催なので、
訪日レイヤーが自国のタイムゾーンから予定を入れても会場の時刻がずれない。
利用者に見せる時刻は必ず JST へ直してから文面にする。

## 7. ガバナンス&セキュリティ設計

1. **素顔とコス名の分離**: アカウントはコス名のみで成立。試着・メイクナビの顔画像は処理後即破棄し、破棄証跡を残す。肌タイプ・顔属性は数値スコアのみ保持。認証は Firebase Authentication に預け、**アプリが受け取るのは不透明な uid だけ**（メールアドレスは認証基盤に留め、Firestore へは書かない）
2. **公平性（バイアス対策）**: 肌色・骨格による工程の個別化は「同じキャラに同じ品質で近づく」ための設計であり、特定の肌タイプを標準扱いしない。生成工程の品質を肌タイプ別に評価するテストセットを用意し、偏りを可観測にする
3. **位置共有の時限性**: 到着見込み共有はイベント当日のみ有効。終了後は履歴ごと自動削除（身バレリスクの構造的遮断）
4. **二次創作ガイドライン・エンジン**: キャラ名・作品名はメイク工程生成の内部入力に限定。生成画像に権利物ロゴ・素材を合成しない検証層を出力前に通す
5. **リスケの承認制**: 撮影枠・集合変更は合わせ主催者の承認を経て確定
6. **通知をアプリ内で閉じる**: 外部メッセージング（LINE・X）は使わない。SNSアカウント・電話番号・端末トークンを一切持たないことで、コス名から素性へ辿る経路そのものを作らない。当日の自律通知も同じお知らせ欄に積む
7. **権限の境界**: 誰であるかは必ずIDトークンから決め、リクエストボディの `layerId` は信用しない。他人の遠征・お知らせ・合わせは参照できない。主催者だけが撮影枠の設置・リスケ確定・メンバー進捗の代理入力を行え、代理入力は監査ログに残る
8. **バッチ用エンドポイントの保護**: PWA を一般公開するため実行基盤側の呼び出し元制限は使えない。スケジューラ用の `/tasks/*` は共有シークレットで守り、**未設定のときは開いたままにせず閉じる**（設定漏れが「誰でも削除できる状態」にならないようにする）

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── compose.yaml              # ローカル開発一式
├── Dockerfile                # API＋エージェント＋PWA配信（Cloud Run へそのまま出す）
├── docs/Dockerfile           # アーキテクチャ図の生成（graphviz + Noto CJK）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

API とエージェントは同一プロセスに置く。MVP の規模では分離の利点よりデプロイ単位が
増える手間が勝るため。分けるときは `agents/` を別サービスに切り出すだけで済むよう、
エージェントは ports にしか依存させない。PWA も同じイメージから静的配信する。

### compose サービス

| サービス | 内容 |
|---|---|
| api | API＋エージェント＋PWA。ホットリロード有効（ソースをボリュームマウント） |
| firestore | Firestoreエミュレータ。api と一緒に起動し、本番データに触れず開発する |
| test | ユニットテスト（`--profile test`）。インメモリで回す |
| test-firestore | Firestoreアダプタの結合テスト（`--profile itest`）。エミュレータに実際に読み書きする |
| diagram | アーキテクチャ図の生成（`--profile docs`） |

- `docker compose up api` で起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### mock / live の差し替え

外部APIはすべてポート越しに呼び、環境変数1本で mock と live を入れ替える。
**キーが無ければ live 指定でも mock に落として起動を続ける**（デモ当日にキー1本で
全部落ちるのを避ける）。ただし次の2つは例外:

- **認証**: `APP_ENV` が local/test 以外で開発用ログイン（パスワード検証なし）を指定すると起動時に落とす
- **データ保存先**: 既定が Firestore。ローカルもエミュレータを使い、インメモリはテストだけで使う。
  プロセスが死ぬと消える保存先を既定にすると、本番だけで出る不具合を取り逃がすため

| 環境変数 | mock（既定） | live |
|---|---|---|
| `VTO_MODE` | ハッシュ由来の決定的な擬似応答 | YouCam API |
| `TRANSIT_MODE` | 主要駅の静的グラフ | 駅すぱあと MCP |
| `LLM_MODE` | キーワード抽出・固定文 | Gemini API |
| `REPOSITORY` | —（既定が Firestore） | Firestore。`memory` はテスト専用 |
| `AUTH_MODE` | 開発用ログイン（**ローカル専用**） | Firebase Authentication |

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | ADK雛形＋駅すぱあとMCP（大荷物制約パラメータ設計） |
| W2 | YouCam試着（ウィッグ・衣装）パイプライン |
| W3 | メイク工程分解（Gemini×Fitzpatrick×顔属性）＋当日ステップUI（日英2言語） |
| W4 | 合わせ管理（進捗・到着監視・リスケ起案） |
| W5 | 認証（Firebase Auth）＋チャットUI＋アプリ内お知らせ |
| W6 | 時限データ削除・監査ログ・PWA仕上げ・デプロイ・デモ動画 |

**MVPで捨てるもの**: 更衣室の実データ連携（混雑予測はイベント規模からのモデル値）、主催者ダッシュボード（画面モック）、中韓言語（日英のみ。設計はi18n前提で追加は設定のみ）、Web Push（当日通知はアプリ内お知らせ欄のみ。アプリを開いていないと気づけない点は承知の上）

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | レイヤー支援サービスの完全空白（検索立証）。肌色・骨格対応のメイク手順化は世界的にも未見 |
| 自律性・エージェントらしさ | 当日モードの自律進行（動線再計算・混雑アラート）＋承認制リスケ。入口はチャット1本で、足りない条件だけを聞き返して一日ぶんを立ち上げる |
| 実装品質と拡張性 | WCS等のインバウンド市場へ多言語で展開可能。イベント主催者向け更衣室DXへの事業拡張。YouCam活用の幅（試着×メイク×肌タイプ×顔属性）が全候補中最大 |

## 11. 追加案（GMI Cloud 活用）

| 追加機能 | 使用モデル | 内容 |
|---|---|---|
| 完成イメージ生成 | Seedream / Flux-kontext-pro | ウィッグ×衣装×メイクの「全部盛り完成予想図」を高品質生成。製作・購入前の意思決定を支える |
| アフタームービー | image-to-video＋動画編集＋音楽生成 | 合わせの撮影写真から遠征のPV/アフタームービーを自動編集。合わせの締めの文化（アフターレポート）をエージェント化 |
| 多言語TTS音声ガイド | minimax / ElevenLabs TTS | 移動中・メイク中は画面を見られない。動線ナビとメイク工程を母語の音声で読み上げ（インバウンド対応の実効性を上げる） |

### ガバナンス上の追加論点

- 完成イメージ・PVにも二次創作ガイドライン・エンジンを適用（権利物ロゴ・素材の合成禁止、AI生成透かし）
- ボイスクローンは使用しない（キャラクター音声の権利リスクを構造的に回避）

### MVPへの影響

- 音声ガイド（TTS）はW3の当日ステップUIに低コストで追加可能。完成イメージ・PVは拡張枠

### 実装状況

3機能とも実装済み。GMI Cloud は画像・動画・音声のどれも同じリクエストキュー
（`/ie/requestqueue/apikey/requests` へ投げて `request_id` をポーリング）なので、
HTTP の面倒は1モジュールに閉じ、3つのポートはモデルIDとペイロードの差だけになっている。

| 機能 | エンドポイント | 状態 |
|---|---|---|
| 完成イメージ | `POST /api/expeditions/{id}/look-image` | モックで通しの動作確認済み |
| 音声ガイド | `POST /api/expeditions/{id}/voice-guide` | 同上。台本は工程・動線から生成 |
| アフタームービー | `POST /api/awase/{id}/after-movie` | 主催者のみ。1枚目からの image-to-video に限定 |

ガバナンス上の追加論点は次の形で担保している。

- **キャラ名・作品名は生成プロンプトにも入れない**（渡すのは色味と造形の記述だけ）。
  §7-4 の「内部入力に限定」を、生成の入力側にも広げた
- 生成物には AI 生成の表示を入れ、モック代替物であることも隠さず示す
- **ボイスクローンは参照音声を受け取る引数を持たない**ことで、型のうえで使えなくしている。
  依頼文にクローンを求める表現があれば 422 で止め、監査ログに残す
- 生成に失敗しても遠征プランは壊れない（生成物は「無くても成立する」ものとして扱う）

未達: 複数枚の動画編集と音楽生成（設計書の記載に対し、image-to-video 1本に留まる）。
