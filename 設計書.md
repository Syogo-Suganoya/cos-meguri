# コスめぐり 設計書

レイヤーの一日エージェント（CANDIDATE 13）

## 1. 概要

コスプレイヤーの遠征の一日ぶんを組み立てる統合エージェント。キャラ再現メイクの手順化と大荷物制約の移動動線を、相談の欄に入れた条件から一度に出す。

- **一言価値**: ばらばらに調べていた「キャラのメイク手順」と「大荷物の経路」を、1回の相談でまとめて出す
- **テーマ適合**: 「素顔と素性を紐づけない」というコミュニティ固有のプライバシー要求を、設計の中心に置く。アカウントに名前すら持たせない
- **市場性**: 世界コスプレサミット（名古屋、40ヶ国以上参加）・acosta!・コミケなど恒常的な大型イベント群に加え、コスプレ目的の訪日（コスプレインバウンド）が拡大中。**多言語・多人種対応**（肌色・骨格の違いに合わせたメイク工程生成）を最初から設計に組み込み、海外レイヤーを一次ターゲットに含める

## 2. 課題

| 課題 | 現状 |
|---|---|
| 更衣室が紙と行列 | 大型イベントは当日券＋長蛇の列。枠予約も混雑予測もない（コミケ・acosta等で確認） |
| メイクが参入障壁 | AIコスプレ「画像生成」は多数あるが、自分の顔に施す**手順**を教えるものは皆無 |
| メイク情報が日本人前提 | 既存のコスメイク情報は日本人の肌色・骨格を暗黙の前提にしており、肌色が濃い・彫りが深いレイヤーはそのまま真似できない。海外レイヤーの実課題 |
| 大荷物移動が過酷 | キャリー＋ウィッグ＋小道具。一般の経路案内は身軽な人前提 |
| 訪日レイヤーの情報障壁 | WCS等で来日する海外レイヤーは、更衣室ルール・移動・イベント慣習を母語で得る手段がない |

## 3. ユースケース

1. **ソロ遠征**: メイク手順ナビ→大荷物動線の行動計画。気に入った工程・経路はお気に入りに残し、次の遠征でも見返す
2. **訪日レイヤー（インバウンド）**: 世界コスプレサミット（名古屋）等への海外からの参加者を母語で伴走。空港→ホテル→会場の大荷物動線、更衣室ルール・イベント慣習の案内、肌色・骨格に合わせたメイク工程
3. **イベント主催者**（拡張）: 更衣室の枠管理・混雑ダッシュボード、多言語案内の自動生成

### 対応イベント（初期収載）

収載は「名前から目的地（最寄り駅）と開始・終了を自動で入れる」ためのマスタ。**収載に無いローカルイベントも、
目的地と開始・終了を相談の欄に入れれば組める**（行きは開始までに着き、帰りは終了後に出る）。

| イベント | 場所 | 特性 |
|---|---|---|
| 世界コスプレサミット（WCS） | 名古屋・大須 | 40ヶ国以上、インバウンドの本丸。市街地回遊型で移動動線が複雑 |
| acosta! | 池袋・ハレザ等 | 高頻度開催。更衣室枠・街歩き動線 |
| コミックマーケット | 東京ビッグサイト | 最大規模。更衣室行列・大荷物・帰路混雑 |
| ホココス | 名古屋・栄 | 道路開放型。地元イベントとして土地勘あり |

## 4. システムアーキテクチャ

```
[PWA (名前を持たないアカウント)]
  ├── ログイン画面（自作UI）──── Firebase Authentication（IDトークン取得。ローカルはエミュレータ）
  ├── 相談（条件の欄）→ プラン     # ログイン不要（ゲスト = 匿名ログイン）。通知は持たない
  └── ☆ お気に入り・マイページ    # ログインした人だけ。ゲストの条件とプランは登録時に引き継ぐ
      │  REST + IDトークン
      ▼
[API Gateway (Cloud Run: api)]   # トークン検証 → uid からアカウントを解決
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── 相談エージェント ──────── 条件を保持→揃ったらプラン起動
  ├── メイクナビエージェント ──── Gemini（言い回し・翻訳）＋ルールベースの工程分解
  ├── 動線エージェント ────────── 駅すぱあと API MCPサーバー（大荷物制約）
  └── 多言語エージェント ──────── Gemini（母語出力・文化的補足）
      │
      ▼
[Firestore]（レイヤー・遠征・相談の条件・監査ログ）
[Cloud Logging]（監査ログ：アカウント紐付け）
```

**通知を持たない理由**: LINE・X 連携は身バレの経路を増やす。当日の通知（遅延・撤収・合わせの連絡）は
機能ごと取り下げたので（§13）、宛先となる電話番号・端末トークン・SNSアカウントを一切持たない。

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | 遠征計画の受付。サブエージェントの結果を1つのプランに束ねる | 利用者の操作で起動 |
| 相談 | 条件（イベント名・日付・目的地・開始・終了・作品名・キャラ名・出発駅・荷物）を欄から受けて保持し、足りない項目を返す。揃った時点で Orchestrator を起動 | **条件はLLMに読み取らせない**（読み違いを確かめる往復を無くす） |
| メイクナビ | 目標キャラの髪色・瞳色・造形→9工程に分解→ステップ表示 | 手順生成は自律 |
| 多言語 | 全UI・案内・メイク工程を利用者の母語で提供（日英中韓＋Geminiで随時拡張）。イベント慣習・更衣室ルールの文化的な補足説明も生成 | 自律 |
| 動線 | 大荷物制約の経路（乗換ぶんを足した体感時間の短い順・乗換の余裕を最大）。開場までに着き、閉場後に帰る | 推奨まで |

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run |
| AI（必須要件） | Gemini API（`gemini-3.7-flash`。メイク工程の言い換え・翻訳・イベント慣習の補足）、Agent Development Kit |
| スポンサー | 駅すぱあと API MCPサーバー |
| 多言語 | Gemini API（翻訳＋文化的補足の生成。UIはi18n、メイク工程は生成時に母語で出力） |
| 認証 | **Firebase Authentication**。ログイン画面は自作し、フロントは Identity Toolkit の REST でIDトークンを取得（JS SDK・CDN非依存）。バックエンドは firebase-admin で検証し、受け取るのは uid のみ。**ローカルも同じ経路**で、Docker の認証エミュレータに繋ぐ |
| データ | Firestore（相談の条件・お気に入りも同居） |
| UI | PWA（HTML/CSS/JS、フレームワーク非依存）。ログイン・相談の欄はすべて自作 |

**外部メッセージング（LINE / X）は使わない。** 通知そのものを持たない（§7-6）。

## 6. データモデル（Firestore）

```
layers/{layerId}          # 名前を持たない。本名・素顔とも紐づけない
  lang, prefs: { 移動制約, ホームイベント }
  auth_uid                # 認証基盤の不透明ID。**メールアドレスは保存しない**
expeditions/{expId}
  event: { event_id, 名称, 会場, 目的地の駅, 日時 }  # 収載イベントは event_id、それ以外は "custom"
  event_date                        # 開催日（JST）。日付1本で引くための冗長列
  character: { 作品名, キャラ名 }   # メイク工程生成にのみ使用
  makeup_steps: [ {order, area, instruction, lang} ]   # キャラの色味・造形から生成
  route: { 行き, 帰り, 荷物モード }
favorites/{favoriteId}          # 気に入ったメイク工程・動線の「写し」
  layer_id, kind: makeup|route, direction, label, exp_id, makeup | route
                                 # 元の遠征を組み直しても消えないよう、中身ごと写す。
                                 # 写しはサーバが本人の遠征から作る。見出しにはキャラ名・作品名を入れる（本人にしか返さない）
chats/{layerId}                  # 1レイヤーにつき1組の条件
  slots: { 条件 }, exp_id          # 旧版の messages は無視。event_id だけの旧版はマスタで目的地・時刻を補って読む
audit/{logId}  # アカウント紐付けの証跡（取り下げた機能の記録も読み戻せる）
```

イベントの時刻はすべて **JST 基準**で組み立てる。収載イベントは日本開催なので、
訪日レイヤーが自国のタイムゾーンから予定を入れても会場の時刻がずれない。
利用者に見せる時刻は必ず JST へ直してから文面にする。

## 7. ガバナンス&セキュリティ設計

1. **素顔と素性の分離**: アカウントは認証基盤の uid だけで成立し、コス名も含めて名前を持たない。**顔写真を受け取る経路をアプリが持たない**（撮る導線も送る経路も無い）。認証は Firebase Authentication に預け、**アプリが受け取るのは不透明な uid だけ**（メールアドレスは認証基盤に留め、Firestore へは書かない）
2. **公平性（バイアス対策）**: 肌色・骨格で工程を分岐させない（判定する手段を持たないので、測っていない値で語らない）。肌の明るさを変える指示は出さず、キャラの色味には光と影で寄せる
3. **位置を受け取らない**: 合わせ（到着見込みの共有）を取り下げたので、位置を扱う経路が無い（§13）
4. **二次創作ガイドライン・エンジン**: キャラ名・作品名はメイク工程生成の内部入力に限定し、遠征の応答など外向きには出さない。**例外は本人にしか返さないお気に入りの見出し**で、並べたときにどのキャラのものか見分けられるよう入れる。生成画像に権利物ロゴ・素材を合成しない検証層を出力前に通す
5. **（取り下げ）リスケの承認制**: 合わせごと取り下げた（§13）
6. **通知を持たない**: 外部メッセージング（LINE・X）もアプリ内のお知らせ欄も持たない。SNSアカウント・電話番号・端末トークンを一切持たないことで、コス名から素性へ辿る経路そのものを作らない
7. **権限の境界**: 誰であるかは必ずIDトークンから決め、リクエストボディの `layerId` は信用しない。他人の遠征・相談の条件・お気に入り・監査ログは参照できない（他人のものは「無い」と同じ 404 を返し、あるかどうかも教えない）。ログインしていない人は匿名ログインの**ゲスト**として相談とプランだけを使い、お気に入りは 403。ゲストの条件とプランを本人へ移すのは、検証済みの**匿名ログインのトークン**を渡されたときだけ
8. **無人で動く経路を持たない**: 定期実行（Cloud Scheduler）は使わない。処理はすべて利用者の操作を起点にする。一般公開の PWA に、呼び出し元を絞れないバッチ入口を残さないため

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── compose.yaml              # ローカル開発一式
├── Dockerfile                # API＋エージェント＋PWA配信（Cloud Run へそのまま出す）
├── docs/Dockerfile           # アーキテクチャ図の生成（graphviz + Noto CJK）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

API とエージェントは同一プロセスに置く。MVP の規模では分離の利点よりデプロイ単位が
増える手間が勝るため。分けるときは `agents/` を別サービスに切り出すだけで済むよう、
エージェントは ports にしか依存させない。PWA も同じイメージから静的配信する。

### compose サービス

| サービス | 内容 |
|---|---|
| api | API＋エージェント＋PWA。ホットリロード有効（ソースをボリュームマウント） |
| firestore | Firestoreエミュレータ。api と一緒に起動し、本番データに触れず開発する |
| firebase-auth | Firebase Authentication エミュレータ。api と一緒に起動し、本番と同じメール＋パスワードのログインを手元で通す |
| shots | 画面操作イメージの撮影（`--profile shots`）。Puppeteer で利用者と同じ順に操作する |
| test | ユニットテスト（`--profile test`）。インメモリで回す |
| test-firestore | Firestoreアダプタの結合テスト（`--profile itest`）。エミュレータに実際に読み書きする |
| diagram | アーキテクチャ図の生成（`--profile docs`） |

- `docker compose up api` で起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### mock / live の差し替え

外部APIはすべてポート越しに呼び、環境変数1本で mock と live を入れ替える。
**キーが無ければ live 指定でも mock に落として起動を続ける**（デモ当日にキー1本で
全部落ちるのを避ける）。ただし次の2つは例外:

- **認証**: ローカルも Firebase Authentication（エミュレータ）。パスワードを検証しないログインは `APP_ENV=test` 以外で、
  エミュレータの変数（署名の無いトークンを通す）は local/test 以外で、起動時に落とす
- **データ保存先**: 既定が Firestore。ローカルもエミュレータを使い、インメモリはテストだけで使う。
  プロセスが死ぬと消える保存先を既定にすると、本番だけで出る不具合を取り逃がすため

| 環境変数 | mock（既定） | live |
|---|---|---|
| `EKISPERT_MODE` | 主要駅の静的グラフ | 駅すぱあと MCP |
| `GEMINI_MODE` | 固定文 | Gemini API |
| `REPOSITORY` | —（既定が Firestore） | Firestore。`memory` はテスト専用 |
| `AUTH_MODE` | —（`dev` はテスト専用） | Firebase Authentication（ローカルはエミュレータ） |

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | ADK雛形＋駅すぱあとMCP（大荷物制約パラメータ設計） |
| W2 | 条件の受付と一括プラン生成 |
| W3 | メイク工程分解（ルールベース＋Gemini の言い換え）＋ステップUI（日英2言語） |
| W4 | 動線の大荷物評価 |
| W5 | 認証（Firebase Auth）＋相談UI |
| W6 | 監査ログ・PWA仕上げ・デプロイ・デモ動画 |

**MVPで捨てるもの**: 更衣室の混雑（§14）、主催者ダッシュボード（画面モック）、中韓言語（日英のみ。設計はi18n前提で追加は設定のみ）、当日の運行情報への追従（§13）、合わせ（§13）

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | レイヤー支援サービスの完全空白（検索立証）。肌色・骨格対応のメイク手順化は世界的にも未見 |
| 自律性・エージェントらしさ | 相談の欄の条件だけから、メイクと動線のエージェントの結果を1つの一日に束ねる。開場時刻から、乗換ぶんの荷物の時間を足して出発時刻を逆算する |
| 実装品質と拡張性 | WCS等のインバウンド市場へ多言語で展開可能。イベント主催者向け更衣室DXへの事業拡張 |

## 11. 見送り: 生成メディア（旧 GMI Cloud 活用案）

完成イメージ生成・アフタームービー・多言語TTS音声ガイドの3機能は、GMI Cloud を
提供元として実装していたが、**提供が受けられなくなったため機能ごと取り下げた**。

代わりのモデルを探して残す選択肢もあったが、採らなかった。3機能とも
「無くても一日ぶんのプランは成立する」ものとして設計しており、
提供元を差し替えるコストに対して、中心の価値（メイク工程・大荷物動線）は
1つも増えないため。

取り下げにあたり、次を一緒に落としている。

- `ports/media.py` と実装（GMI・モック）、`agents/visual.py`、`agents/voice.py`
- `MediaAsset` / `MediaKind`、`Expedition.look_image` / `voice_guides`、`Awase.after_movie`
- 「できあがり」ページと、左端の帯の同名の節
- ガードレールのうち音声クローン検査（`screen_voice_request`）。**止める対象の
  機能が無くなったので、約束としても残さない**（README からも消した）

権利物のロゴ・素材の合成を止める検査（`screen_generation_request`）は残る。
いまは呼び出し元が無いので、検査そのものも取り下げた。

`AuditAction` の `MEDIA_GENERATED` / `VOICE_CLONE_BLOCKED` は値だけ残した。
もう誰も書かないが、Firestore に文字列で入った過去の記録を読み戻せなくなるため。

---

## 12. 見送り: 顔解析と試着（旧 YouCam 活用案）

ウィッグ・衣装のバーチャル試着と、Fitzpatrick 肌タイプ・顔属性による
メイク工程の個別化は、YouCam（Perfect Corp）を提供元として設計していたが、
**提供を外したため機能ごと取り下げた**。

取り下げの理由は提供元の都合だけではない。実キーで叩いて分かったこと:

- **顔属性・顔比率の解析はこの契約に存在しない**（14通りの名前を試して全て 404）。
  つまり「骨格に応じた個別化」は当初から実データの裏付けを持てなかった
- **衣装の試着も存在しない**（髪は hair-style / hair-color があるが、
  template_id のカタログが要る）
- 肌タイプ判定（`fitzpatrick-scale-analyzer`）だけは実在し、疎通も確認した

肌タイプだけを自己申告で残す案も検討したが、採らなかった。**測っていない値で
「あなたの顔立ちに合わせました」と言わない**ことを優先し、メイク工程は
キャラの髪色・瞳色・造形だけから組む形に寄せた。濃さの判断は利用者に残す。

取り下げにあたり、次を一緒に落としている。

- `ports/vto.py` と実装（YouCam・モック）、`agents/fitting.py`
- `FaceProfile` / `FaceAttributes` / `FitzpatrickType` / `FittingResult` 一式、
  `Layer.face_profile`、`Expedition.fitting`、`MakeupPlan.fitzpatrick_type`
- `POST /api/me/face` と `POST /api/fitting`、準備ページの顔登録カメラ・
  ファイル選択・試着カード
- 権利物のロゴ合成を止める検査（`screen_generation_request`）。生成画像を
  作る経路が無くなり、呼び出し元が消えたため

`AuditAction` の `FACE_IMAGE_DISCARDED` / `FITTING_IMAGE_DISCARDED` /
`IP_GUARD_BLOCKED` は値だけ残した。もう誰も書かないが、Firestore に文字列で
入った過去の記録を読み戻せなくなるため。

**副作用として、プライバシーの主張は強くなった。** 「顔画像は解析後に破棄する」
から「**顔写真を受け取る経路そのものが無い**」へ変わっている。

---

## 13. 見送り: 当日モード・合わせ・自由文の相談・準備ページ

実際に触ってみて、次の4つを機能ごと取り下げた。

**運行情報（遅延）と、その通知。** 駅すぱあとの MCP には運行情報の Tool が無く、
REST のレスキューナウは契約外で 403 だった。つまり当日ページの「いまの状況を見る」は、
押しても「運行情報は取れていません」としか言えなかった。取れない値で「乱れなし」とも
言えないので、遅延の再計算・撤収のお知らせを含む当日モードごと落とした。

**合わせ（複数人の段取り）。** 招集・進捗・到着監視・撮影枠のリスケ起案と、主催者の承認。
到着監視は各自のプランの経路と遅延を材料にする設計だったが、遅延が取れず、遠征と
合わせを結ぶ `Expedition.awase_id` もどこからも設定されていなかった。材料の無いまま
画面だけが残っていた。

**自由文の相談。** 「9/6のコミケに横浜駅から行きます」と書くと条件を読み取る入口。
読み取った結果は結局6つの欄で確かめて直す必要があり、欄を直接埋めれば読み違いが
起きない。入口を欄に一本化し、「この条件で組む」でプランへ移るようにした。

**準備ページ。** 中身は「コス名を変える」だけになっていた。コス名は合わせで呼ばれるための
名前だったので、合わせを落とすと変える理由が無くなる。ログイン時の仮の名前のままでよい。

取り下げにあたり、次を一緒に落としている。

- `agents/awase.py` `domain/awase.py` `domain/parsing.py`、`ports/notifier.py` と `adapters/in_app_notifier.py`
- `Awase` / `AwaseMember` / `Shoot` / `RescheduleProposal` / `LocationShare` / `MemberProgress`、
  `Notification` / `NotificationKind`、`ChatMessage` / `ChatRole`、`ServiceDisruption`、
  `Expedition.awase_id`、`Layer.is_pending`
- `TransitPort.disruptions`、`LlmPort.extract_slots`、`RouteAgent.recheck`、`Orchestrator.run_day_of`
- `POST /api/chat` `POST /api/chat/reset` `/api/me/notifications*` `POST /api/expeditions/{id}/day-of` `/api/awase*`
- `/prep` `/day` のページ、左端の帯の同名の節、看板のお知らせ（ベル）
- 招待だけされた未ログインのアカウント（pending）を、あとからログインした本人に引き継ぐ処理

`AuditAction` の `LOCATION_SHARE_*` / `RESCHEDULE_*` / `PROGRESS_UPDATED_BY_ORGANIZER` と
`ExpeditionStatus.DAY_OF` は値だけ残した。Firestore に文字列で入った過去の記録を読み戻せなく
なるため。旧版で保存した `chats` の `messages` は、読み込み時に無視している（テストあり）。

**副作用として、プライバシーの主張はさらに強くなった。** 「位置の共有は当日だけ」から
「**位置を受け取る経路そのものが無い**」へ、「通知はアプリ内で閉じる」から
「**通知を持たない**」へ変わっている。

---

## 14. 見送り: 更衣室の混雑予測とレイヤー名（ローカルの開発用ログインも）

**更衣室の混雑予測。** イベント規模ごとの想定人数（コミケ 12,000人など）と、手で入れた
部屋数・定員から、30分ごとの待ち時間を見積もって入る・出る時刻をすすめていた。実データは
1つも使っておらず、画面では「予測」と断っていたが、数字の出どころが私たちの置いた値しか
無いのに時刻まで言い切るのは、§12 で決めた「測っていない値で語らない」と噛み合わない。
機能ごと落とし、動線は「開場までに着き、閉場後に出る」だけで組むようにした。

**レイヤー名（コス名）。** 合わせで呼ばれるための名前だったが、合わせを落としたので
表示にしか使われていなかった。名前の欄があるだけで、コス名から素性へ辿る手がかりになる。
アカウントは uid だけで成立させ、名前は持たない。

**ローカルの開発用ログイン。** ボタン1つで端末ごとの仮の名前で入る実装だったが、
フォームの無いログイン画面は何をしているのか分からず、本番のログインを手元で試す手段も
無かった。ローカルも Firebase Authentication に揃え、Docker の認証エミュレータに繋ぐ。
パスワードを検証しない実装はテストの中にだけ残した（`APP_ENV=test` 以外では起動を止める）。
**エミュレータの変数は本番で署名の無いトークンを通してしまう**ので、local/test 以外では起動を止める。

取り下げにあたり、次を一緒に落としている。

- `domain/crowd.py`、`agents/dressing.py`、`LlmPort.explain`
- `DressingPlan` / `DressingSlot` / `CrowdLevel`、`Expedition.dressing`、
  `EventMaster.dressing_rooms` / `dressing_capacity` / `expected_cosplayers`、`ExpeditionCreate.attendance_factor`
- プランの「更衣室」タブ、トップの特徴カード
- `Layer.handle`、`AuthIdentity.suggested_handle`、`SessionRequest`、`LayerUpdate.handle`、看板の名前表示
- ログイン画面の「お試しで入る」と、端末ごとの仮の名前

旧版で保存したドキュメントの `dressing` と `handle` は、読み込み時に無視している（テストあり）。
